Run lint, test, commit, and push.

1. Run `golangci-lint run ./...`
2. Run `go test ./...`
3. If both pass, create a new branch if on main
4. Commit all changes with a good commit message
5. Push to origin
